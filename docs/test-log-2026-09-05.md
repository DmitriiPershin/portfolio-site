# Verification log — 2026-09-05

## Revision and scope

- Runtime and regression-test commit: `a8cd64f` (`fix: use single authored contours and stabilize portfolio layout`).
- Branch: `main`. Runtime revision pushed successfully to GitHub.
- Visual evidence: `artifacts/qa-2026-09-05/` (generated from the production server build of that revision).
- Exact source-style evidence: `reports/figma-border-audit-2026-09-05.md`. Earlier “solid Figma border” claims are superseded by that raw-property audit.

## Technical results

| Check | Result |
|---|---|
| `npm run build` (includes Astro/TypeScript check) | 24 files; 0 errors, 0 warnings, 0 hints; server build complete |
| `npm test -- --workers=2` | 60 passed; 18 intentional breakpoint-specific skips; 0 failures; 41.7 s |
| Desktop / tablet / mobile | 1440×900 / 1024×768 / 390×844; no horizontal overflow or runtime console errors |
| Single-border regression | Every generated card owns one path; native paint transparent; zero legacy glow overlays or added app/Process icon rings |
| Pointer and mobile scroll | Same gradient changes, path stays fixed; per-card phases differ; frame-based hover sampling proves gradual activation |
| Source-style regression | Process gradient includes transparent stop; Hard inset spread−17.68; exact Workflow/app frame dimensions retained |
| Localization | RU→EN and persistence pass; original chip contains exactly one active route-label language |
| Typography | Delayed-font test waits before hero reveal; negative40 px ink allowance persists; logo bounds unchanged after reveal; all signature faces load |
| Responsive menu/header/chips | All eight menu links fit1280×600; hover opacity1; mobile header/logo alignment and full-width rows pass |
| Counters | Only250 uses1.8 s; Interfaces retain0.9 s; accessible final values correct |
| Fallbacks | Reduced-motion and no-JavaScript content remain readable at all three widths |
| Screenshot capture | Completed reduced-motion section/overlay captures and full-motion hero/scroll-glow captures |
| `git diff --check` | Passed |

The initial runs exposed test-harness issues: CSS minification serializes `0.9s` as `.9s`, and automation round trips made a wall-clock hover sample unreliable under load. The assertions now compare equivalent values and sample actual browser frames. A clean full run passed without retries. An initial browser-start timeout did not recur in the clean run.

## Visual checks performed

- Desktop Process: a single smoothed green contour fades out along the authored gradient; original icon frames have no outer duplicate.
- Desktop EN Workflow: Context, Context interpretation and Styles occupy their original chips, without Russian text underneath; connectors retain their authored positions.
- Mobile Skills: paired and single chips fill each row; Soft Skills remains visible.
- Desktop menu: all eight entries are visible; responsive fit is also tested at1280×600.
- Mobile hero and desktop mid-reveal: dots and glyph overhang are visible; full-viewport captures avoid the misleading crop from screenshotting only the layout box.
- Mobile Process scroll: one contour rotates its light with the scroll; no second radius appears.

## Short product check for Dmitrii

1. Reload the live hero with cache disabled, then compare its beginning/end on desktop and mobile.
2. Move the pointer around Process/Skills, then scroll the same blocks on mobile. Check whether the new single-contour light feels right.
3. Switch to English and inspect the three localized Workflow chips. Open the menu on a short desktop window and check all eight entries.

Spacing normalization and final perceptual approval remain product decisions, not automatically applied Figma changes.

## Verified production deployment

- Uploaded from the clean pushed `main` checkout `ac1ced7`; runtime remains `a8cd64f`.
- Railway service: `portfolio`, environment: `production`.
- Deployment: `7ed12718-11af-464a-bdd4-1072b5fbf512`; status: `SUCCESS`.
- Live page and `/api/health`: HTTP200; health body `{ "status": "ok" }`.
- Live CSS `index.CMtren7T.css` and client JS `index.astro_astro_type_script_index_0_lang.gdJbgP8X.js` match the local production build's content-hashed assets.
- `PORTFOLIO_BASE_URL=https://portfolio-production-e2d0.up.railway.app npm test -- --workers=2`: **60 passed, 18 breakpoint-specific skips, 0 failures**, 2.1 min. Includes desktop, tablet, mobile, RU/EN, overlays, fonts/cold reveal, single-contour animation, full-width chips and no-JavaScript/reduced-motion checks.
- This final log update is documentation-only; it does not change the tested runtime.
